# Global CLAUDE.md

## Environment

- Shell: zsh (Homebrew installed at `/opt/homebrew`)
- The following CLI tools are installed and available via Homebrew: `gh`, `gog`, `jq`, `fzf`
- Do not attempt to install tools that are already available on PATH
