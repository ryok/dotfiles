*~
.DS_Store

**/.claude/settings.local.json

# 環境変数・シークレット
# リポジトリ側の .gitignore 漏れに対する保険として global 側でも塞ぐ。
# コミット対象の .env.sample / .env.example は影響を受けない。
.env
.env.local
.envrc
.direnv/

# ツールの生成物
# ctags の出力はリポジトリ直下に出る。アンカー無しの `tags` にすると
# tags/ をソースディレクトリに持つプロジェクトを丸ごと無視してしまう。
/tags
